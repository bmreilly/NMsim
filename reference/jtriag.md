# Column numbers of elements in a triangular representation of a symmetric matrix

Column numbers of elements in a triangular representation of a symmetric
matrix

## Usage

``` r
jtriag(blocksize, istart = 1, diag = "lower")
```

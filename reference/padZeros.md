# pad zeros on integers

pad zeros on integers

## Usage

``` r
padZeros(x, nchars)
```

## Arguments

- x:

  integers to pad. They can be coded as characters already.

- nchars:

  Optional specification of length of character strings to return. If
  not supplied, characters will be padded to match length of max value
  in x.

## Value

A character vector

# read one sim element. This will be run in lapply in NMreadSim.

read one sim element. This will be run in lapply in NMreadSim.

## Usage

``` r
NMreadSimResOne(x)
```

## Arguments

- x:

  A path to an fst file or a data set

## Value

A data.table
